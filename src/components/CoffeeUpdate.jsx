/* eslint-disable no-unused-vars */
import React from 'react';

const CoffeeUpdate = () => {
    return (
        <div>
            Update a coffee
        </div>
    );
};

export default CoffeeUpdate;